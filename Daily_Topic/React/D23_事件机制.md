### 事件机制

```css
React提供了一种“顶层注册，事件收集，统一触发”的事件机制

"顶层注册"：将事件统一绑定在Root根元素上，与之前的绑定在document上不同

"事件收集"： 指的是事件触发时（实际上是root上的事件处理函数被执行），构造合成事件对象，
按照冒泡或捕获的路径去组件中收集真正的事件处理函数。

这套机制规避了无法将事件直接绑定到DOM节点上的问题，并且能够很好地利用fiber树的层级关系来生成事件执行路径，进而模拟事件捕获和冒泡.

另外还带来两个非常重要的特性：
1.对事件进行归类，可以在事件产生的任务上包含不同的优先级
2.提供合成事件对象，抹平浏览器的兼容性差异
```

```css
事件注册：V17是将事件注册到root上而不是Document上了，这主要是为了渐近升级，避免多版本的React共存的
场景中事件系统发生冲突


```
